# Same as requirements.txt but without torch/torchvision.
# Use this on RunPod where the container already has torch+CUDA.

# ---- Segmentation stack ----
segmentation-models-pytorch>=0.4
timm>=1.0
transformers>=4.40,<5

# ---- Data / augmentation / CV ----
numpy>=1.26,<3
opencv-python-headless>=4.8
albumentations>=2.0
Pillow>=10.0

# ---- Metrics / math ----
scipy>=1.11
scikit-image>=0.22
scikit-learn>=1.3
networkx>=3.0

# ---- Config / progress / logging ----
pyyaml>=6.0
rich>=13.0
tqdm>=4.65
matplotlib>=3.8

# ---- Dev ----
pytest>=7.0
ruff>=0.4
