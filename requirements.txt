# ---- Core DL ----
# RunPod PyTorch images already provide torch + torchvision with CUDA.
# Do NOT reinstall torch here — it will break the CUDA linkage.
# If on a bare image, install separately:
#   pip install torch==2.8.0 torchvision==0.23.0 --index-url https://download.pytorch.org/whl/cu124
torch>=2.1,<3
torchvision>=0.16,<1

# ---- Segmentation stack ----
segmentation-models-pytorch>=0.4
timm>=1.0
transformers>=4.40,<5

# ---- Data / augmentation / CV ----
numpy>=1.26,<3
opencv-python-headless>=4.8
albumentations>=2.0
Pillow>=10.0

# ---- Metrics / math ----
scipy>=1.11
scikit-image>=0.22
scikit-learn>=1.3
networkx>=3.0

# ---- Config / progress / logging ----
pyyaml>=6.0
rich>=13.0
tqdm>=4.65
matplotlib>=3.8

# ---- Dev ----
pytest>=7.0
ruff>=0.4
